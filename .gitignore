# Dependencies, build, and environment
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.env
.env.local
*.log
dist/
build/
*.egg-info/

# Excluded from check-in (local only)
.cursor/rules/
docs/

# Vendored Vosk speech model (~40 MB) — fetched via `npm run fetch:voice-model`
public/vosk/model.tar.gz

# Packaged app artifact (reproducible via `npx evenhub pack`)
*.ehpk

# IDE and OS
.idea/
.vscode/
.DS_Store
*.swp
*~
